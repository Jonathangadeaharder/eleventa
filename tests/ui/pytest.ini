[pytest]
# Run tests one at a time to isolate issues (fixed syntax)
addopts = -xvs

# Enable verbose output is now included in addopts (-v)

# Show local variables in tracebacks is now included in addopts (-s)

# Timeout settings
timeout = 5
timeout_method = thread

# Disable warning capture to see all warnings
filterwarnings = all 