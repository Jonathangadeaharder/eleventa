[pytest]
# Run tests one at a time to isolate issues
xvs = 1

# Enable verbose output
verbose = true

# Show local variables in tracebacks
showlocals = true

# Timeout settings
timeout = 5
timeout_method = thread

# Disable warning capture to see all warnings
filterwarnings = all 